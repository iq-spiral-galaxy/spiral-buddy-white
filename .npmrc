only-built-dependencies[]=esbuild
only-built-dependencies[]=electron
# pnpm 기본 symlinked node_modules에서는 electron-builder가
# transitive deps (minimatch, wsl-utils 등)를 누락시키는 경우가 잦음.
# hoisted로 평탄화하면 npm과 동일한 구조 → electron-builder가 정확히 트레이스.
node-linker=hoisted
